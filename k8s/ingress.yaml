apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nionpulse
spec:
  rules:
    - host: nionpulse.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api-service
                port:
                  number: 80
