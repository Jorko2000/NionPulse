# NionPulse
It ingests events through a REST API, publishes them to AWS SNS/SQS, processes them asynchronously using threaded and multiprocessing workers, stores results in Postgres/DynamoDB/S3, and exposes analytics dashboards.
